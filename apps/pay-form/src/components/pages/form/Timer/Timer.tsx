import './Timer.scoped.scss';

export const FormTimer: React.FC = () => {
    return (
        <div className="timer">
            <p className="timer__text">The invoice is active for 10:09</p>
        </div>
    );
};
